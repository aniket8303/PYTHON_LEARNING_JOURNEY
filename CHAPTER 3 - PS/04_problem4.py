# Q) Replace the double space from problem 3 with single spaces.
text = "Aniket is  a powerful man"

print(text.replace("  "," "))


print(text) #Strings are immutable which means that you cannot change them by running functions on them